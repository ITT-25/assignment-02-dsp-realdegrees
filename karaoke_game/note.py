class Note:
    """Wraps a pyglet object and adds per note metadata"""
    # TODO: add metadata like completion (completion represents the amount of time the "voice cursor" has been on the note relative to the duration of the note)
    # TODO: adjust visual representation of the note based on the completion
    # ? Gradient can probably achieved easily with pillow but could be unnecessaryly expensive, alternatively use 2 objects per note and represent the completion by scaling them
    pass